# Query

QueryToken:
  name: token
  in: query
  description: Validate token
  required: true
  type: string

LoginRequest:
  type: object
  properties:
    useremail:
      type: string
    password:
      type: string
